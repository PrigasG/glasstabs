% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glass_theme.R
\name{glass_theme}
\alias{glass_theme}
\title{Create a custom color theme for glasstabs widgets}
\usage{
glass_theme(
  bg_color = NULL,
  border_color = NULL,
  text_color = NULL,
  accent_color = NULL
)
}
\arguments{
\item{bg_color}{Background color of the trigger button and dropdown
panel. Default: \code{NULL} (inherits the dark preset).}

\item{border_color}{Border color. Default: \code{NULL}.}

\item{text_color}{Main text color. Default: \code{NULL}.}

\item{accent_color}{Accent color used for the animated tick, badge,
checked-state highlights, and the "Clear all" link. Default: \code{NULL}.}
}
\value{
A named list of class \code{"glass_theme"} consumed by
\code{\link[=glassMultiSelect]{glassMultiSelect()}}.
}
\description{
A helper function — inspired by ggplot2's \code{theme()} — that lets you
override only the colors you care about. Any argument you leave out
falls back to sensible defaults so you are never forced to supply all
four values.
}
\details{
Pass the result directly to the \code{theme} argument of \code{\link[=glassMultiSelect]{glassMultiSelect()}}.
}
\examples{
# Only change the accent — everything else stays dark
glassMultiSelect("x", c(A = "a", B = "b"),
  theme = glass_theme(accent_color = "#f59e0b")
)

# Light panel, blue accent
glassMultiSelect("x", c(A = "a", B = "b"),
  theme = glass_theme(
    bg_color     = "rgba(255,255,255,0.98)",
    border_color = "rgba(0,0,0,0.10)",
    text_color   = "#1e293b",
    accent_color = "#2563eb"
  )
)

}
